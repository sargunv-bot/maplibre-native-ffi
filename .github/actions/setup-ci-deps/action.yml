name: Setup CI deps
description: Install sccache and mise toolchain

runs:
  using: composite
  steps:
    - name: Enable Git long paths
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: git config --global core.longpaths true

    - name: Cache MapLibre Native source
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          third_party/maplibre-native
          .git/modules/third_party/maplibre-native
        key: mln-src-v2-${{ hashFiles('.gitmodules', 'cmake/mln_version.cmake') }}

    - name: Install sccache
      uses: mozilla-actions/sccache-action@9e7fa8a12102821edf02ca5dbea1acd0f89a2696 # v0.0.10

    - name: Use Linux software graphics drivers
      if: ${{ runner.os == 'Linux' }}
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install --no-install-recommends --yes \
          libegl-mesa0 \
          libgl1-mesa-dri \
          libvulkan1 \
          mesa-utils \
          mesa-utils-extra \
          mesa-vulkan-drivers
        {
          echo "EGL_PLATFORM=surfaceless"
          echo "LIBGL_ALWAYS_SOFTWARE=true"
        } >> "$GITHUB_ENV"

    - name: Use Windows Vulkan software driver
      if: ${{ runner.os == 'Windows' && endsWith(env.MISE_ENV, '-vulkan') }}
      uses: ./.github/actions/setup-windows-vulkan-software

    - name: Use Windows OpenGL software driver
      if: ${{ runner.os == 'Windows' && endsWith(env.MISE_ENV, '-wgl') }}
      uses: ./.github/actions/setup-windows-opengl-software

    - name: Cache Cargo packages
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
        key: ${{ runner.os }}-${{ runner.arch }}-cargo-registry-v1-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache OpenHarmony SDK
      if: ${{ contains(env.MISE_ENV, 'ohos') }}
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: ~/setup-ohos-sdk
        key: ohos-sdk-v1-v6.1-${{ runner.os }}-${{ runner.arch }}

    - name: Setup OpenHarmony SDK
      if: ${{ contains(env.MISE_ENV, 'ohos') }}
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        sdk_root="$HOME/setup-ohos-sdk"
        archive="ohos-sdk-windows_linux-public.tar.gz"

        mkdir -p "$sdk_root"
        cd "$sdk_root"
        if [[ ! -f "$sdk_root/linux/native/build/cmake/ohos.toolchain.cmake" ]]; then
          gh release download v6.1 \
            --repo openharmony-rs/ohos-sdk \
            --pattern "${archive}*"

          cat "${archive}".a* > "$archive"
          expected_sha="$(tr -d '[:space:]' < "${archive}.sha256")"
          actual_sha="$(sha256sum "$archive" | awk '{print $1}')"
          if [[ "$actual_sha" != "$expected_sha" ]]; then
            echo "OHOS SDK archive hash mismatch: expected $expected_sha, got $actual_sha" >&2
            exit 1
          fi
          tar -xf "$archive"
          unzip -q linux/native-*.zip -d linux
        fi

        echo "OHOS_SDK_NATIVE=$sdk_root/linux/native" >> "$GITHUB_ENV"

    - name: Cache Gradle
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-${{ runner.arch }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-${{ runner.arch }}-gradle-

    - name: Trim Windows PATH before mise activation
      if: ${{ runner.os == 'Windows' }}
      shell: pwsh
      run: |
        $workspaceDrive = Split-Path -Qualifier $env:GITHUB_WORKSPACE
        # Git Bash maps /tmp to the checkout drive; create it before mise hooks run.
        New-Item -ItemType Directory -Force -Path (Join-Path $workspaceDrive "tmp") | Out-Null

        $paths = @(
          if ($env:SCCACHE_PATH) { Split-Path -Parent $env:SCCACHE_PATH }
          if ($env:Path) { $env:Path -split ';' }
          "C:\Program Files\Git\bin",
          "C:\Program Files\Git\usr\bin",
          "$env:SystemRoot\System32",
          "$env:SystemRoot",
          "$env:SystemRoot\System32\Wbem",
          "$env:SystemRoot\System32\WindowsPowerShell\v1.0",
          "C:\Program Files\PowerShell\7",
          "C:\Program Files\Git\cmd"
        ) | Where-Object { $_ -and (Test-Path $_) }

        "Path=$($paths -join ';')" >> $env:GITHUB_ENV

        $gradleTemp = $env:RUNNER_TEMP -replace '\\', '/'
        $gradleOptions = "-Djava.io.tmpdir=$gradleTemp"
        if ($env:GRADLE_OPTS) {
          $gradleOptions = "$gradleOptions $env:GRADLE_OPTS"
        }
        "GRADLE_OPTS=$gradleOptions" >> $env:GITHUB_ENV

    # Windows ARM64 gets Vulkan libraries through mise/pixi, but glslangValidator
    # is not available there yet; the Vulkan examples need it to precompile shaders.
    - name: Install Windows ARM64 Vulkan SDK
      if: ${{ env.MISE_ENV == 'windows-arm64-vulkan' }}
      uses: jakoch/install-vulkan-sdk-action@b394a1abed9cb019d8e637eeadac93d73e0853da # v1.5.5
      with:
        vulkan_version: 1.4.341.0
        cache: true
        stripdown: true

    - name: Install mise toolchain
      uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v4.0.1
      with:
        cache: true
        cache_key_prefix: mise-v2
        install: true

    - name: Wire sccache into CMake
      shell: bash
      run: |
        mise_env="${MISE_ENV:-$(mise exec -- printenv MISE_ENV)}"
        {
          echo "SCCACHE_GHA_ENABLED=true"
          echo "SCCACHE_GHA_VERSION=${mise_env%-*}"
          echo "SCCACHE_BASEDIRS=${GITHUB_WORKSPACE}"
          echo "CMAKE_C_COMPILER_LAUNCHER=sccache"
          echo "CMAKE_CXX_COMPILER_LAUNCHER=sccache"
        } >> "$GITHUB_ENV"

    # The mise action may restore Rust install metadata without the rustup
    # toolchain, components, or cross targets from mise.toml. Reinstall rust
    # from mise config instead of redeclaring components/targets here.
    - name: Ensure Rust toolchain
      shell: bash
      run: MISE_NO_HOOKS=1 mise install rust || MISE_NO_HOOKS=1 mise install --force rust
